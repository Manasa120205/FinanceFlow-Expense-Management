# Production Frontend Environment Configuration
# When hosted on Vercel with serverless Python functions, leave blank to use relative same-origin /api/v1
# If deploying backend to an external cloud URL (e.g. Render/Railway), set it here:
# VITE_API_URL=https://your-backend.onrender.com/api/v1
